# CSS (Cascading Style Sheets) 

เป็นภาษาที่ใช้กำหนดรูปแบบการแสดงผลของ HTML โดยช่วยควบคุมการออกแบบเว็บให้สวยงามและใช้งานได้ดีขึ้น


---

📌 **พื้นฐานของ CSS** 
CSS มี 3 วิธีหลักในการนำมาใช้กับ HTML:
 
1. **Inline CSS**  – กำหนดสไตล์โดยตรงในแท็ก HTML
 
2. **Internal CSS**  – กำหนดสไตล์ภายใน `<style>` ใน HTML
 
3. **External CSS**  – ใช้ไฟล์ `.css` แยกออกจาก HTML
**1. Inline CSS** เป็นการกำหนดสไตล์โดยตรงในแท็ก HTML ผ่าน `style="..."`
**ตัวอย่าง:** 

```html
<p style="color: red; font-size: 20px;">ข้อความสีแดง ขนาด 20px</p>
```


---

**2. Internal CSS** ใช้แท็ก `<style>` ภายใน `<head>` ของ HTML
**ตัวอย่าง:** 

```html
<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Internal CSS</title>
    <style>
        p {
            color: blue;
            font-size: 18px;
        }
    </style>
</head>
<body>
    <p>ข้อความนี้เป็นสีน้ำเงิน และขนาด 18px</p>
</body>
</html>
```


---

**3. External CSS** แยกไฟล์ `.css` ออกมา แล้วเรียกใช้งานผ่าน `<link>`
**
CSS (Cascading Style Sheets) เป็นภาษาที่ใช้กำหนดรูปแบบการแสดงผลของ HTML โดยช่วยควบคุมการออกแบบเว็บให้สวยงามและใช้งานได้ดีขึ้น


---

📌 **พื้นฐานของ CSS** 
CSS มี 3 วิธีหลักในการนำมาใช้กับ HTML:
 
1. **Inline CSS**  – กำหนดสไตล์โดยตรงในแท็ก HTML
 
2. **Internal CSS**  – กำหนดสไตล์ภายใน `<style>` ใน HTML
 
3. **External CSS**  – ใช้ไฟล์ `.css` แยกออกจาก HTML
**1. Inline CSS** เป็นการกำหนดสไตล์โดยตรงในแท็ก HTML ผ่าน `style="..."`
**ตัวอย่าง:** 

```html
<p style="color: red; font-size: 20px;">ข้อความสีแดง ขนาด 20px</p>
```


---

**2. Internal CSS** ใช้แท็ก `<style>` ภายใน `<head>` ของ HTML
**ตัวอย่าง:** 

```html
<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Internal CSS</title>
    <style>
        p {
            color: blue;
            font-size: 18px;
        }
    </style>
</head>
<body>
    <p>ข้อความนี้เป็นสีน้ำเงิน และขนาด 18px</p>
</body>
</html>
```


---

**3. External CSS** แยกไฟล์ `.css` ออกมา แล้วเรียกใช้งานผ่าน `<link>`
ไฟล์ `style.css`** 

```css
p {
    color: green;
    font-size: 22px;
}
```
**
CSS (Cascading Style Sheets) เป็นภาษาที่ใช้กำหนดรูปแบบการแสดงผลของ HTML โดยช่วยควบคุมการออกแบบเว็บให้สวยงามและใช้งานได้ดีขึ้น


---

📌 **พื้นฐานของ CSS** 
CSS มี 3 วิธีหลักในการนำมาใช้กับ HTML:
 
1. **Inline CSS**  – กำหนดสไตล์โดยตรงในแท็ก HTML
 
2. **Internal CSS**  – กำหนดสไตล์ภายใน `<style>` ใน HTML
 
3. **External CSS**  – ใช้ไฟล์ `.css` แยกออกจาก HTML
**1. Inline CSS** เป็นการกำหนดสไตล์โดยตรงในแท็ก HTML ผ่าน `style="..."`
**ตัวอย่าง:** 

```html
<p style="color: red; font-size: 20px;">ข้อความสีแดง ขนาด 20px</p>
```


---

**2. Internal CSS** ใช้แท็ก `<style>` ภายใน `<head>` ของ HTML
**ตัวอย่าง:** 

```html
<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Internal CSS</title>
    <style>
        p {
            color: blue;
            font-size: 18px;
        }
    </style>
</head>
<body>
    <p>ข้อความนี้เป็นสีน้ำเงิน และขนาด 18px</p>
</body>
</html>
```


---

**3. External CSS** แยกไฟล์ `.css` ออกมา แล้วเรียกใช้งานผ่าน `<link>`
**
CSS (Cascading Style Sheets) เป็นภาษาที่ใช้กำหนดรูปแบบการแสดงผลของ HTML โดยช่วยควบคุมการออกแบบเว็บให้สวยงามและใช้งานได้ดีขึ้น


---

📌 **พื้นฐานของ CSS** 
CSS มี 3 วิธีหลักในการนำมาใช้กับ HTML:
 
1. **Inline CSS**  – กำหนดสไตล์โดยตรงในแท็ก HTML
 
2. **Internal CSS**  – กำหนดสไตล์ภายใน `<style>` ใน HTML
 
3. **External CSS**  – ใช้ไฟล์ `.css` แยกออกจาก HTML
**1. Inline CSS** เป็นการกำหนดสไตล์โดยตรงในแท็ก HTML ผ่าน `style="..."`
**ตัวอย่าง:** 

```html
<p style="color: red; font-size: 20px;">ข้อความสีแดง ขนาด 20px</p>
```


---

**2. Internal CSS** ใช้แท็ก `<style>` ภายใน `<head>` ของ HTML
**ตัวอย่าง:** 

```html
<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Internal CSS</title>
    <style>
        p {
            color: blue;
            font-size: 18px;
        }
    </style>
</head>
<body>
    <p>ข้อความนี้เป็นสีน้ำเงิน และขนาด 18px</p>
</body>
</html>
```


---

**3. External CSS** แยกไฟล์ `.css` ออกมา แล้วเรียกใช้งานผ่าน `<link>`
ไฟล์ `style.css`** 

```css
p {
    color: green;
    font-size: 22px;
}
```
ไฟล์ `index.html`** 

```html
<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>External CSS</title>
    <link rel="stylesheet" href="style.css">
</head>
<body>
    <p>ข้อความนี้เป็นสีเขียว ขนาด 22px</p>
</body>
</html>
```


---

🎨 **Selectors (ตัวเลือกใน CSS)** 
Selectors ใช้เพื่อเลือกองค์ประกอบ HTML ที่ต้องการกำหนดสไตล์
| Selector | คำอธิบาย | ตัวอย่าง | 
| --- | --- | --- | 
| * | เลือกทุกองค์ประกอบ | * { margin: 0; padding: 0; } | 
| p | เลือก <p> ทั้งหมด | p { color: blue; } | 
| .class | เลือกคลาสที่กำหนด | .red-text { color: red; } | 
| #id | เลือก ID ที่กำหนด | #title { font-size: 30px; } | 
| element, element | เลือกหลายองค์ประกอบ | h1, h2 { color: green; } | 
| element > child | เลือกลูกโดยตรง | div > p { color: orange; } | 
| element element | เลือกลูกทั้งหมดภายใน | div p { font-size: 18px; } | 
| element:hover | เมื่อโฮเวอร์เมาส์ | button:hover { background-color: yellow; } | 
**ตัวอย่าง:** 

```css
p {
    color: blue;
}
.red-text {
    color: red;
}
#title {
    font-size: 30px;
}
```


---

🎨 **Box Model** 
Box Model ใน CSS ประกอบด้วย 4 ส่วนหลัก:
 
1. **Content**  - เนื้อหาภายในกล่อง
 
2. **Padding**  - ระยะห่างระหว่างขอบกล่องกับเนื้อหา
 
3. **Border**  - เส้นขอบของกล่อง
 
4. **Margin**  - ระยะห่างระหว่างกล่องกับองค์ประกอบอื่น
**ตัวอย่าง:** 

```css
.box {
    width: 200px;
    height: 100px;
    padding: 10px;
    border: 2px solid black;
    margin: 20px;
}
```


---

🎨 **Flexbox (การจัดการ Layout ง่ายๆ)** Flexbox เป็นเทคนิคที่ช่วยจัดการเลย์เอาต์ให้ง่ายขึ้น
**ตัวอย่าง:** 

```css
.container {
    display: flex;
    justify-content: space-between;
}
.item {
    width: 100px;
    height: 100px;
    background-color: lightblue;
    text-align: center;
    line-height: 100px;
}
```


```html
<div class="container">
    <div class="item">1</div>
    <div class="item">2</div>
    <div class="item">3</div>
</div>
```


---

🚀 **Grid Layout (การสร้าง Layout ซับซ้อน)** CSS Grid ใช้สำหรับสร้างเลย์เอาต์ที่ซับซ้อน
**ตัวอย่าง:** 

```css
.grid-container {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 10px;
}
.grid-item {
    background-color: lightcoral;
    padding: 20px;
    text-align: center;
}
```


```html
<div class="grid-container">
    <div class="grid-item">1</div>
    <div class="grid-item">2</div>
    <div class="grid-item">3</div>
</div>
```


---

🎭 **CSS Animations** สามารถสร้างอนิเมชันได้โดยใช้ `@keyframes`
**ตัวอย่าง:** 

```css
@keyframes move {
    from { transform: translateX(0); }
    to { transform: translateX(100px); }
}

.box {
    width: 100px;
    height: 100px;
    background-color: red;
    animation: move 2s infinite alternate;
}
```


```html
<div class="box"></div>
```


---

📚 **สรุป** 
- CSS ใช้ปรับแต่ง HTML ให้สวยงามและจัดการ Layout ได้ง่ายขึ้น

- มี 3 วิธีในการใช้ CSS: Inline, Internal, External
 
- ใช้ **Selectors**  เพื่อเลือกองค์ประกอบ
 
- **Box Model**  เป็นพื้นฐานของขนาดและระยะห่าง
 
- **Flexbox และ Grid**  ใช้จัดเลย์เอาต์ของเว็บ
 
- **CSS Animation**  ทำให้เว็บดูมีชีวิตชีวา


---
